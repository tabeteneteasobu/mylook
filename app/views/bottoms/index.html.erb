<h2>My bottoms</h2>

<ul class='item-lists'>
  <% @category3.each do |category| %>
      <li class='list'>
        <div class ='item-img-content'>
        <%= link_to(bottom_path(category.id), class: '#') do %>
          <%= image_tag category.image.variant(resize: '300x300'), class: "item-img" %>
        <% end %>
        </div>
      </li>
  <% end %>
</ul>